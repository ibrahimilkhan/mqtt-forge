# Throwaway keys and certificates, regenerated by make-certs.sh. Never committed: a CA key in
# a public repository is a CA key anybody can sign with.
certs/
# Written by make-certs.sh's sibling step in up.sh
mosquitto/passwd
